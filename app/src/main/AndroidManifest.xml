<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.crazyit.auction.client"
   >

    <!--允许联网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--获取GSM（2g）、WCDMA（联通3g）等网络状态的信息  -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--获取wifi网络状态的信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!--保持CPU 运转，屏幕和键盘灯有可能是关闭的,用于文件上传和下载 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!--获取sd卡写的权限，用于文件上传和下载-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!--允许读取手机状态 用于创建BmobInstallation-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppBase">
        <activity
            android:name=".Login"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AuctionClientActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".ViewItem"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".ManageKind"
            android:label="@string/manage_kind"></activity>
        <activity
            android:name=".AddKind"
            android:label="@string/add_kind_title"></activity>
        <activity
            android:name=".ManageItem"
            android:label="@string/manage_item"></activity>
        <activity
            android:name=".AddItem"
            android:label="@string/add_item_title"></activity>
        <activity
            android:name=".ViewBid"
            android:label="@string/view_bid"></activity>
        <activity
            android:name=".ChooseKind"
            android:label="@string/choose_kind"></activity>
        <activity
            android:name=".ChooseItem"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".AddBid"
            android:label="@string/item_detail_title"></activity>
        <activity
            android:name=".Register"
            android:label="@string/title_activity_register"></activity>
    </application>

</manifest>