<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/btn_common">

    <ImageView
        android:id="@+id/iv_kind"
        android:layout_width="@dimen/dp_100"
        android:layout_height="@dimen/dp_100"
        android:layout_margin="@dimen/dp_16"
        android:scaleType="centerCrop"
        android:layout_centerVertical="true"
        android:src="@drawable/ic_launcher"/>

    <TextView
        android:id="@+id/tv_kind_name"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp_36"
        android:layout_alignTop="@+id/iv_kind"
        android:layout_marginRight="@dimen/dp_8"
        android:layout_toRightOf="@+id/iv_kind"
        android:maxLines="2"
        android:text="玩具"
        android:textColor="@color/tv_value"
        android:textSize="@dimen/text_size_14"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/tv_kind_desc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tv_kind_name"
        android:layout_alignLeft="@+id/tv_kind_name"
        android:textColor="@color/tv_value"
        android:textSize="@dimen/text_size_14"/>

</RelativeLayout>